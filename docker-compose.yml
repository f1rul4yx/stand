services:
  stand:
    image: f1rul4yx/stand:latest
    container_name: stand
    restart: always
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - TZ=Europe/Madrid
      - NODE_ENV=production
